// ⚠️  AUTO-GENERATED — do not edit by hand.
// Source of truth: eerie-rpc/src/lib.rs  (roam service trait)
// Regenerate with: pnpm codegen
//
// Transport: TCP (roam-stream on Rust side, @bearcove/roam-tcp on TS side).

// @generated by roam-codegen
// DO NOT EDIT - regenerate with `cargo xtask codegen --typescript`

import type { MethodHandler, Connection, MessageTransport, DecodeResult, MethodSchema } from "@bearcove/roam-core";
import {
  encodeResultOk, encodeResultErr, encodeInvalidPayload,
  concat, encodeVarint, decodeVarintNumber, decodeRpcResult,
  encodeWithSchema, decodeWithSchema,
  encodeBool, decodeBool,
  encodeU8, decodeU8, encodeI8, decodeI8,
  encodeU16, decodeU16, encodeI16, decodeI16,
  encodeU32, decodeU32, encodeI32, decodeI32,
  encodeU64, decodeU64, encodeI64, decodeI64,
  encodeF32, decodeF32, encodeF64, decodeF64,
  encodeString, decodeString,
  encodeBytes, decodeBytes,
  encodeOption, decodeOption,
  encodeVec, decodeVec,
  encodeTuple2, decodeTuple2, encodeTuple3, decodeTuple3,
  encodeEnumVariant, decodeEnumVariant,
  helloExchangeInitiator, defaultHello,
} from "@bearcove/roam-core";
import { Server as RoamTcpServer } from "@bearcove/roam-tcp";
import { RpcError } from "@bearcove/roam-core";

export const METHOD_ID = {
  ping: 0xf9ac3c559a17c24bn,
  fileRead: 0x55af1ab81e2c641fn,
  fileWrite: 0xa00fceeb19b9aec1n,
  circuitParseYaml: 0x2c96f6b5723b9f30n,
  circuitToYaml: 0xc2546eeb4e4b321dn,
  circuitNew: 0x3838810c2e3c1236n,
  simDc: 0x6daa42a183d66408n,
} as const;

// Named type definitions
export interface Position {
  x: number;
  y: number;
}

export type PropertyValue =
  | { tag: 'Float'; value: number }
  | { tag: 'Int'; value: bigint }
  | { tag: 'Bool'; value: boolean }
  | { tag: 'String'; value: string };

export interface ComponentInstance {
  id: string;
  type_id: string;
  label: string | null;
  position: Position;
  rotation: number;
  flip_x: boolean;
  properties: Map<string, PropertyValue>;
}

export interface Point {
  x: number;
  y: number;
}

export interface WireSegment {
  start: Point;
  end: Point;
}

export interface PinRef {
  component_id: string;
  pin_id: string;
}

export interface NetLabel {
  name: string;
  position: Point;
}

export interface Net {
  id: string;
  name: string | null;
  segments: WireSegment[];
  pins: PinRef[];
  labels: NetLabel[];
}

export interface CircuitMetadata {
  created_at: string;
  modified_at: string;
  author: string | null;
}

export interface Circuit {
  id: string;
  name: string;
  description: string | null;
  version: string;
  components: ComponentInstance[];
  nets: Net[];
  metadata: CircuitMetadata;
}

export type AnalysisType =
  | { tag: 'Dc' }
  | { tag: 'Ac'; frequency_hz: number }
  | { tag: 'Transient'; time_s: number };

export interface SimulationResult {
  node_voltages: Map<string, number>;
  branch_currents: Map<string, number>;
  converged: boolean;
  analysis_type: AnalysisType;
}

// Request/Response type aliases
export type PingRequest = [];
export type PingResponse = string;

export type FileReadRequest = [string];
export type FileReadResponse = { ok: true; value: string } | { ok: false; error: string };

export type FileWriteRequest = [
  string, // path
  string, // content
];
export type FileWriteResponse = { ok: true; value: void } | { ok: false; error: string };

export type CircuitParseYamlRequest = [string];
export type CircuitParseYamlResponse = { ok: true; value: Circuit } | { ok: false; error: string };

export type CircuitToYamlRequest = [Circuit];
export type CircuitToYamlResponse = { ok: true; value: string } | { ok: false; error: string };

export type CircuitNewRequest = [string];
export type CircuitNewResponse = { ok: true; value: Circuit } | { ok: false; error: string };

export type SimDcRequest = [Circuit];
export type SimDcResponse = { ok: true; value: SimulationResult } | { ok: false; error: string };

// Caller interface for EerieDaemon
export interface EerieDaemonCaller {
  /**  Health check — returns "pong". */
  ping(): Promise<string>;
  /**  Read a file from the filesystem. */
  fileRead(path: string): Promise<{ ok: true; value: string } | { ok: false; error: string }>;
  /**  Write a file to the filesystem. */
  fileWrite(path: string, content: string): Promise<{ ok: true; value: void } | { ok: false; error: string }>;
  /**  Parse a YAML circuit file and return the Circuit. */
  circuitParseYaml(yaml: string): Promise<{ ok: true; value: Circuit } | { ok: false; error: string }>;
  /**  Serialize a Circuit to YAML text. */
  circuitToYaml(circuit: Circuit): Promise<{ ok: true; value: string } | { ok: false; error: string }>;
  /**  Create a new empty circuit with the given name. */
  circuitNew(name: string): Promise<{ ok: true; value: Circuit } | { ok: false; error: string }>;
  /**  Run DC operating-point analysis on a circuit. */
  simDc(circuit: Circuit): Promise<{ ok: true; value: SimulationResult } | { ok: false; error: string }>;
}

// Client implementation for EerieDaemon
export class EerieDaemonClient<T extends MessageTransport = MessageTransport> implements EerieDaemonCaller {
  private conn: Connection<T>;

  constructor(conn: Connection<T>) {
    this.conn = conn;
  }

  /**  Health check — returns "pong". */
  async ping(): Promise<string> {
    const schema = eerieDaemon_schemas.ping;
    const payload = new Uint8Array(0);
    const response = await this.conn.call(0xf9ac3c559a17c24bn, payload);
    const offset = decodeRpcResult(response, 0);
    const result = decodeWithSchema(response, offset, schema.returns).value;
    return result as string;
  }

  /**  Read a file from the filesystem. */
  async fileRead(path: string): Promise<{ ok: true; value: string } | { ok: false; error: string }> {
    const schema = eerieDaemon_schemas.fileRead;
    const payload = encodeWithSchema(path, schema.args[0]);
    const response = await this.conn.call(0x55af1ab81e2c641fn, payload);
    try {
      const offset = decodeRpcResult(response, 0);
      const value = decodeWithSchema(response, offset, schema.returns).value;
      return { ok: true, value } as { ok: true; value: string } | { ok: false; error: string };
    } catch (e) {
      if (e instanceof RpcError && e.isUserError() && e.payload && schema.error) {
        const error = decodeWithSchema(e.payload, 0, schema.error).value;
        return { ok: false, error } as { ok: true; value: string } | { ok: false; error: string };
      }
      throw e;
    }
  }

  /**  Write a file to the filesystem. */
  async fileWrite(path: string, content: string): Promise<{ ok: true; value: void } | { ok: false; error: string }> {
    const schema = eerieDaemon_schemas.fileWrite;
    const payload = encodeWithSchema([path, content], { kind: 'tuple', elements: schema.args });
    const response = await this.conn.call(0xa00fceeb19b9aec1n, payload);
    try {
      const offset = decodeRpcResult(response, 0);
      const value = decodeWithSchema(response, offset, schema.returns).value;
      return { ok: true, value } as { ok: true; value: void } | { ok: false; error: string };
    } catch (e) {
      if (e instanceof RpcError && e.isUserError() && e.payload && schema.error) {
        const error = decodeWithSchema(e.payload, 0, schema.error).value;
        return { ok: false, error } as { ok: true; value: void } | { ok: false; error: string };
      }
      throw e;
    }
  }

  /**  Parse a YAML circuit file and return the Circuit. */
  async circuitParseYaml(yaml: string): Promise<{ ok: true; value: Circuit } | { ok: false; error: string }> {
    const schema = eerieDaemon_schemas.circuitParseYaml;
    const payload = encodeWithSchema(yaml, schema.args[0]);
    const response = await this.conn.call(0x2c96f6b5723b9f30n, payload);
    try {
      const offset = decodeRpcResult(response, 0);
      const value = decodeWithSchema(response, offset, schema.returns).value;
      return { ok: true, value } as { ok: true; value: Circuit } | { ok: false; error: string };
    } catch (e) {
      if (e instanceof RpcError && e.isUserError() && e.payload && schema.error) {
        const error = decodeWithSchema(e.payload, 0, schema.error).value;
        return { ok: false, error } as { ok: true; value: Circuit } | { ok: false; error: string };
      }
      throw e;
    }
  }

  /**  Serialize a Circuit to YAML text. */
  async circuitToYaml(circuit: Circuit): Promise<{ ok: true; value: string } | { ok: false; error: string }> {
    const schema = eerieDaemon_schemas.circuitToYaml;
    const payload = encodeWithSchema(circuit, schema.args[0]);
    const response = await this.conn.call(0xc2546eeb4e4b321dn, payload);
    try {
      const offset = decodeRpcResult(response, 0);
      const value = decodeWithSchema(response, offset, schema.returns).value;
      return { ok: true, value } as { ok: true; value: string } | { ok: false; error: string };
    } catch (e) {
      if (e instanceof RpcError && e.isUserError() && e.payload && schema.error) {
        const error = decodeWithSchema(e.payload, 0, schema.error).value;
        return { ok: false, error } as { ok: true; value: string } | { ok: false; error: string };
      }
      throw e;
    }
  }

  /**  Create a new empty circuit with the given name. */
  async circuitNew(name: string): Promise<{ ok: true; value: Circuit } | { ok: false; error: string }> {
    const schema = eerieDaemon_schemas.circuitNew;
    const payload = encodeWithSchema(name, schema.args[0]);
    const response = await this.conn.call(0x3838810c2e3c1236n, payload);
    try {
      const offset = decodeRpcResult(response, 0);
      const value = decodeWithSchema(response, offset, schema.returns).value;
      return { ok: true, value } as { ok: true; value: Circuit } | { ok: false; error: string };
    } catch (e) {
      if (e instanceof RpcError && e.isUserError() && e.payload && schema.error) {
        const error = decodeWithSchema(e.payload, 0, schema.error).value;
        return { ok: false, error } as { ok: true; value: Circuit } | { ok: false; error: string };
      }
      throw e;
    }
  }

  /**  Run DC operating-point analysis on a circuit. */
  async simDc(circuit: Circuit): Promise<{ ok: true; value: SimulationResult } | { ok: false; error: string }> {
    const schema = eerieDaemon_schemas.simDc;
    const payload = encodeWithSchema(circuit, schema.args[0]);
    const response = await this.conn.call(0x6daa42a183d66408n, payload);
    try {
      const offset = decodeRpcResult(response, 0);
      const value = decodeWithSchema(response, offset, schema.returns).value;
      return { ok: true, value } as { ok: true; value: SimulationResult } | { ok: false; error: string };
    } catch (e) {
      if (e instanceof RpcError && e.isUserError() && e.payload && schema.error) {
        const error = decodeWithSchema(e.payload, 0, schema.error).value;
        return { ok: false, error } as { ok: true; value: SimulationResult } | { ok: false; error: string };
      }
      throw e;
    }
  }

}

/**
 * Connect to a EerieDaemon server over WebSocket.
 * @param url - WebSocket URL (e.g., "ws://localhost:9000")
 * @returns A connected EerieDaemonClient instance
 */
export async function connectEerieDaemon(url: string): Promise<EerieDaemonClient> {
  const server = new RoamTcpServer();
  const connection = await server.connect(url);
  return new EerieDaemonClient(connection);
}

// Handler interface for EerieDaemon
export interface EerieDaemonHandler {
  ping(): Promise<string> | string;
  fileRead(path: string): Promise<{ ok: true; value: string } | { ok: false; error: string }> | { ok: true; value: string } | { ok: false; error: string };
  fileWrite(path: string, content: string): Promise<{ ok: true; value: void } | { ok: false; error: string }> | { ok: true; value: void } | { ok: false; error: string };
  circuitParseYaml(yaml: string): Promise<{ ok: true; value: Circuit } | { ok: false; error: string }> | { ok: true; value: Circuit } | { ok: false; error: string };
  circuitToYaml(circuit: Circuit): Promise<{ ok: true; value: string } | { ok: false; error: string }> | { ok: true; value: string } | { ok: false; error: string };
  circuitNew(name: string): Promise<{ ok: true; value: Circuit } | { ok: false; error: string }> | { ok: true; value: Circuit } | { ok: false; error: string };
  simDc(circuit: Circuit): Promise<{ ok: true; value: SimulationResult } | { ok: false; error: string }> | { ok: true; value: SimulationResult } | { ok: false; error: string };
}

// Method handlers for EerieDaemon
export const eerieDaemon_methodHandlers = new Map<bigint, MethodHandler<EerieDaemonHandler>>([
  [0xf9ac3c559a17c24bn, async (handler, payload) => {
    try {
      const buf = payload;
      let offset = 0;
      if (offset !== buf.length) throw new Error("args: trailing bytes");
      const result = await handler.ping();
      return encodeResultOk(encodeString(result));
    } catch (e) {
      return encodeResultErr(encodeInvalidPayload());
    }
  }],
  [0x55af1ab81e2c641fn, async (handler, payload) => {
    try {
      // Channeling method - use streamingDispatch() instead of simple RPC dispatch
      return encodeResultErr(encodeInvalidPayload());
    } catch (e) {
      return encodeResultErr(encodeInvalidPayload());
    }
  }],
  [0xa00fceeb19b9aec1n, async (handler, payload) => {
    try {
      // Channeling method - use streamingDispatch() instead of simple RPC dispatch
      return encodeResultErr(encodeInvalidPayload());
    } catch (e) {
      return encodeResultErr(encodeInvalidPayload());
    }
  }],
  [0x2c96f6b5723b9f30n, async (handler, payload) => {
    try {
      // Channeling method - use streamingDispatch() instead of simple RPC dispatch
      return encodeResultErr(encodeInvalidPayload());
    } catch (e) {
      return encodeResultErr(encodeInvalidPayload());
    }
  }],
  [0xc2546eeb4e4b321dn, async (handler, payload) => {
    try {
      // Channeling method - use streamingDispatch() instead of simple RPC dispatch
      return encodeResultErr(encodeInvalidPayload());
    } catch (e) {
      return encodeResultErr(encodeInvalidPayload());
    }
  }],
  [0x3838810c2e3c1236n, async (handler, payload) => {
    try {
      // Channeling method - use streamingDispatch() instead of simple RPC dispatch
      return encodeResultErr(encodeInvalidPayload());
    } catch (e) {
      return encodeResultErr(encodeInvalidPayload());
    }
  }],
  [0x6daa42a183d66408n, async (handler, payload) => {
    try {
      // Channeling method - use streamingDispatch() instead of simple RPC dispatch
      return encodeResultErr(encodeInvalidPayload());
    } catch (e) {
      return encodeResultErr(encodeInvalidPayload());
    }
  }],
]);

// Method schemas for runtime encoding/decoding and channel binding
export const eerieDaemon_schemas: Record<string, MethodSchema> = {
  ping: { args: [], returns: { kind: 'string' }, error: null },
  fileRead: { args: [{ kind: 'string' }], returns: { kind: 'string' }, error: { kind: 'string' } },
  fileWrite: { args: [{ kind: 'string' }, { kind: 'string' }], returns: { kind: 'struct', fields: {} }, error: { kind: 'string' } },
  circuitParseYaml: { args: [{ kind: 'string' }], returns: { kind: 'struct', fields: { 'id': { kind: 'string' }, 'name': { kind: 'string' }, 'description': { kind: 'option', inner: { kind: 'string' } }, 'version': { kind: 'string' }, 'components': { kind: 'vec', element: { kind: 'struct', fields: { 'id': { kind: 'string' }, 'type_id': { kind: 'string' }, 'label': { kind: 'option', inner: { kind: 'string' } }, 'position': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } }, 'rotation': { kind: 'i32' }, 'flip_x': { kind: 'bool' }, 'properties': { kind: 'map', key: { kind: 'string' }, value: { kind: 'enum', variants: [{ name: 'Float', fields: { kind: 'f64' } }, { name: 'Int', fields: { kind: 'i64' } }, { name: 'Bool', fields: { kind: 'bool' } }, { name: 'String', fields: { kind: 'string' } }] } } } } }, 'nets': { kind: 'vec', element: { kind: 'struct', fields: { 'id': { kind: 'string' }, 'name': { kind: 'option', inner: { kind: 'string' } }, 'segments': { kind: 'vec', element: { kind: 'struct', fields: { 'start': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } }, 'end': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } } } } }, 'pins': { kind: 'vec', element: { kind: 'struct', fields: { 'component_id': { kind: 'string' }, 'pin_id': { kind: 'string' } } } }, 'labels': { kind: 'vec', element: { kind: 'struct', fields: { 'name': { kind: 'string' }, 'position': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } } } } } } } }, 'metadata': { kind: 'struct', fields: { 'created_at': { kind: 'string' }, 'modified_at': { kind: 'string' }, 'author': { kind: 'option', inner: { kind: 'string' } } } } } }, error: { kind: 'string' } },
  circuitToYaml: { args: [{ kind: 'struct', fields: { 'id': { kind: 'string' }, 'name': { kind: 'string' }, 'description': { kind: 'option', inner: { kind: 'string' } }, 'version': { kind: 'string' }, 'components': { kind: 'vec', element: { kind: 'struct', fields: { 'id': { kind: 'string' }, 'type_id': { kind: 'string' }, 'label': { kind: 'option', inner: { kind: 'string' } }, 'position': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } }, 'rotation': { kind: 'i32' }, 'flip_x': { kind: 'bool' }, 'properties': { kind: 'map', key: { kind: 'string' }, value: { kind: 'enum', variants: [{ name: 'Float', fields: { kind: 'f64' } }, { name: 'Int', fields: { kind: 'i64' } }, { name: 'Bool', fields: { kind: 'bool' } }, { name: 'String', fields: { kind: 'string' } }] } } } } }, 'nets': { kind: 'vec', element: { kind: 'struct', fields: { 'id': { kind: 'string' }, 'name': { kind: 'option', inner: { kind: 'string' } }, 'segments': { kind: 'vec', element: { kind: 'struct', fields: { 'start': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } }, 'end': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } } } } }, 'pins': { kind: 'vec', element: { kind: 'struct', fields: { 'component_id': { kind: 'string' }, 'pin_id': { kind: 'string' } } } }, 'labels': { kind: 'vec', element: { kind: 'struct', fields: { 'name': { kind: 'string' }, 'position': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } } } } } } } }, 'metadata': { kind: 'struct', fields: { 'created_at': { kind: 'string' }, 'modified_at': { kind: 'string' }, 'author': { kind: 'option', inner: { kind: 'string' } } } } } }], returns: { kind: 'string' }, error: { kind: 'string' } },
  circuitNew: { args: [{ kind: 'string' }], returns: { kind: 'struct', fields: { 'id': { kind: 'string' }, 'name': { kind: 'string' }, 'description': { kind: 'option', inner: { kind: 'string' } }, 'version': { kind: 'string' }, 'components': { kind: 'vec', element: { kind: 'struct', fields: { 'id': { kind: 'string' }, 'type_id': { kind: 'string' }, 'label': { kind: 'option', inner: { kind: 'string' } }, 'position': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } }, 'rotation': { kind: 'i32' }, 'flip_x': { kind: 'bool' }, 'properties': { kind: 'map', key: { kind: 'string' }, value: { kind: 'enum', variants: [{ name: 'Float', fields: { kind: 'f64' } }, { name: 'Int', fields: { kind: 'i64' } }, { name: 'Bool', fields: { kind: 'bool' } }, { name: 'String', fields: { kind: 'string' } }] } } } } }, 'nets': { kind: 'vec', element: { kind: 'struct', fields: { 'id': { kind: 'string' }, 'name': { kind: 'option', inner: { kind: 'string' } }, 'segments': { kind: 'vec', element: { kind: 'struct', fields: { 'start': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } }, 'end': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } } } } }, 'pins': { kind: 'vec', element: { kind: 'struct', fields: { 'component_id': { kind: 'string' }, 'pin_id': { kind: 'string' } } } }, 'labels': { kind: 'vec', element: { kind: 'struct', fields: { 'name': { kind: 'string' }, 'position': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } } } } } } } }, 'metadata': { kind: 'struct', fields: { 'created_at': { kind: 'string' }, 'modified_at': { kind: 'string' }, 'author': { kind: 'option', inner: { kind: 'string' } } } } } }, error: { kind: 'string' } },
  simDc: { args: [{ kind: 'struct', fields: { 'id': { kind: 'string' }, 'name': { kind: 'string' }, 'description': { kind: 'option', inner: { kind: 'string' } }, 'version': { kind: 'string' }, 'components': { kind: 'vec', element: { kind: 'struct', fields: { 'id': { kind: 'string' }, 'type_id': { kind: 'string' }, 'label': { kind: 'option', inner: { kind: 'string' } }, 'position': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } }, 'rotation': { kind: 'i32' }, 'flip_x': { kind: 'bool' }, 'properties': { kind: 'map', key: { kind: 'string' }, value: { kind: 'enum', variants: [{ name: 'Float', fields: { kind: 'f64' } }, { name: 'Int', fields: { kind: 'i64' } }, { name: 'Bool', fields: { kind: 'bool' } }, { name: 'String', fields: { kind: 'string' } }] } } } } }, 'nets': { kind: 'vec', element: { kind: 'struct', fields: { 'id': { kind: 'string' }, 'name': { kind: 'option', inner: { kind: 'string' } }, 'segments': { kind: 'vec', element: { kind: 'struct', fields: { 'start': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } }, 'end': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } } } } }, 'pins': { kind: 'vec', element: { kind: 'struct', fields: { 'component_id': { kind: 'string' }, 'pin_id': { kind: 'string' } } } }, 'labels': { kind: 'vec', element: { kind: 'struct', fields: { 'name': { kind: 'string' }, 'position': { kind: 'struct', fields: { 'x': { kind: 'i32' }, 'y': { kind: 'i32' } } } } } } } } }, 'metadata': { kind: 'struct', fields: { 'created_at': { kind: 'string' }, 'modified_at': { kind: 'string' }, 'author': { kind: 'option', inner: { kind: 'string' } } } } } }], returns: { kind: 'struct', fields: { 'node_voltages': { kind: 'map', key: { kind: 'string' }, value: { kind: 'f64' } }, 'branch_currents': { kind: 'map', key: { kind: 'string' }, value: { kind: 'f64' } }, 'converged': { kind: 'bool' }, 'analysis_type': { kind: 'enum', variants: [{ name: 'Dc', fields: null }, { name: 'Ac', fields: { 'frequency_hz': { kind: 'f64' } } }, { name: 'Transient', fields: { 'time_s': { kind: 'f64' } } }] } } }, error: { kind: 'string' } },
};

